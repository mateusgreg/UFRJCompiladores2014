#include <string>
#include <iostream.h>
#include <teste>

char a, b[12], c;
double c;

int f( int a, int b  );

void foo(int y[5], int x)
{
  char y[9];

  printf( "%d", y );
}

int main ()
{
  int a;

  temp = b("32");
  temp = temp && 1;
  b[9] = temp;
  b(1,2,3,temp);
  a = ! temp;
  if( a ) goto i10;
  scanf( "%d", &a ); 
  printf( "Teste", 0 );
i10: return 0;
}
